apiVersion: v1
kind: Service
metadata:
  name: {{ include "tv-cat2-service.fullname" . }}
  labels:
    {{- include "tv-cat2-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tv-cat2-service.selectorLabels" . | nindent 4 }}
